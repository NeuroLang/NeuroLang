# Architecture

NeuroLang is a Python library for probabilistic logic programming for neuroscience research. It is a pure Python package (no C extensions in the core -- though the `pysdd` dependency has C extensions).

## Package Structure

- `neurolang/` - Main package
  - `__init__.py` - Exports + version via `importlib.metadata.version("neurolang")`
  - `frontend/` - Datalog parser and AST (uses `lark` parser library)
  - `probabilistic/` - Probabilistic reasoning (uses `pysdd` for SDD compilation)
  - `type_system/` - Type system (uses `typing_inspect`)
  - `utils/` - Utilities including:
    - `server/` - Tornado-based web server (`neuro-server` console script)
    - `server/neurolang-web/` - Vue.js frontend app (npm-built, distributed as package data)
    - `relational_algebra_set/` - Optional dask-sql backend (uses `sqlalchemy`)

## Build System (Post-Migration, branch migrate-pyproject-uv)

- `pyproject.toml` - Primary config: [build-system], [project], [project.optional-dependencies], [project.scripts], [tool.*]
- `setup.py` - Custom commands only: NPMBuildCommand, DevelopCommand (--dask), InstallCommand (--dask), BuildPyCommand. No distutils imports.
- `setup.cfg` - Retained but only has a stale [versioneer] section (harmless dead config) — no [metadata] or [options]
- `MANIFEST.in` - Cleaned of versioneer references (only includes pyproject.toml)
- `neurolang/_version_scm.py` - Auto-generated by setuptools-scm at install time (do not commit)
- **Removed**: `versioneer.py`, `neurolang/_version.py`, `.coveragerc` (coverage config moved to pyproject.toml)

## setuptools-scm Configuration

- TOML section name is `[tool.setuptools_scm]` (underscore, NOT hyphen)
- Use `version_file` key (NOT the deprecated `write_to` key, which was removed in setuptools-scm 8.0)
- Example: `[tool.setuptools_scm]\nversion_file = "neurolang/_version_scm.py"`

## Key Dependencies (Post-Migration)

| Package | Constraint | Note |
|---------|------------|------|
| lark | >=1.1.2 | Upper bound removed |
| nilearn | >=0.10.0 | Upper bound removed |
| sqlalchemy | >=1.4 | Upper bound removed (used only in optional dask backend) |
| pytest | >=7.0 | In dev extras |
| numpydoc | (no upper bound) | In doc extras |
| typing_inspect | unpinned | Works on 3.12+ with version guard in type_system/__init__.py |
| pysdd | unpinned | C extension, tested on 3.12+ |

## Versioning

Uses `setuptools-scm` with `importlib.metadata` for runtime version access. Version is dynamic and derived from git tags. `neurolang/__init__.py` uses `importlib.metadata.version("neurolang")` with a `PackageNotFoundError` fallback to `"unknown"`.

## Test Infrastructure

- `pytest` with custom markers (`slow`), conftest.py has dask-related fixtures
- Coverage config in pyproject.toml [tool.coverage.*]
- `pytest.ini` still exists at repo root (should be deleted; currently takes precedence over [tool.pytest.ini_options] in pyproject.toml — content is identical so no behavioral difference)
- CI: Originally CircleCI on Python 3.8, 3.9, 3.10. Target: Python 3.12, 3.13, 3.14 via uv.

## Entry Points

- `neuro-server` -> `neurolang.utils.server.app:main`
